package mobi.espier.utils.colorpicker;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

public class ColorPickerPage extends LinearLayout {
	public ColorPickerPage(Context context) {
		super(context);
	}

	public ColorPickerPage(Context context, AttributeSet attr) {
		this(context, attr, 0);
	}

	public ColorPickerPage(Context context, AttributeSet attr, int defStyle) {
		super(context, attr, defStyle);
	}
}
